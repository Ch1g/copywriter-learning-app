<!DOCTYPE html>
<html>
  <head>
    <title>CopywriterLearningApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "Курс копирайтера", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Модули", course_modules_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <div class="container mt-4 flex-shrink-0">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="mt-auto py-3 text-center text-lg-start bg-dark text-muted">
      <div class="text-center p-4 text-light" style="background-color: rgba(0, 0, 0, 0.05);">
        © 2024 CopywriterLearningApp. All rights reserved.
      </div>
    </footer>
  </body>
</html>
